machine:
  node:
    version: 5.5.0
general:
  branches:
    ignore:
      - gh-pages
dependencies:
  post:
    - node_modules/.bin/typings install
test:
  override:
    - npm run build # temporarily disable tests
deployment:
  production:
    branch: master
    commands:
      - npm run deploy