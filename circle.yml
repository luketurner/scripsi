machine:
  node:
    version: 5.5.0
general:
  branches:
    ignore:
      - gh-pages
dependencies:
  post:
    - node_modules/.bin/typings install
deployment:
  production:
    branch: master
    commands:
      - npm run build
      - npm run deploy